import styled from 'styled-components';

const Wrapper = styled.div`
  display: flex;
  flex-direction: column;
  margin-left: 2rem;
  margin-right: 2rem;
`;

const Comment = styled.div`
  flex: 1;
  font-size: 1.6rem;
`;

const style = { Wrapper, Comment };

export default style;
