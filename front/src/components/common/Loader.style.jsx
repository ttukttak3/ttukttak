import styled from 'styled-components';

const LoaderWrap = styled.div`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;

const style = { LoaderWrap };
export default style;
